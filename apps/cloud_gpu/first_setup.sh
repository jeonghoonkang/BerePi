# 필요 파일 설치
apt install screen
apt install htop
apt install tree
apt install rsync
apt install vim
apt install docker-compose

# rsync 사용법
# rsync -e 'ssh -p {포트번호}' -avhz -r --progress --partial {아이디}@{주소}:{경로} {다운로드할 경로}
