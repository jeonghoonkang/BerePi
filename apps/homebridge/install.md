reference : https://github.com/nfarina/homebridge

1) install nodejs
`sudo apt-get install nodejs npm`

2) install homebridge
`sudo apt install libavahi-compat-libdnssd-dev`
`sudo npm install -g homebridge`

