# Path to a service-account JSON file (recommended for a server).
GOOGLE_APPLICATION_CREDENTIALS=/secure/path/service-account.json
# Optional alternative: a short-lived OAuth access token.
GOOGLE_OAUTH_ACCESS_TOKEN=
