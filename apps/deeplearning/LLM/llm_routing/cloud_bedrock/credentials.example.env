# Copy the values into the service environment. Do not commit real secrets.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# Only required for temporary STS credentials:
AWS_SESSION_TOKEN=
# Alternative to the three IAM credential values above:
AWS_BEARER_TOKEN_BEDROCK=
