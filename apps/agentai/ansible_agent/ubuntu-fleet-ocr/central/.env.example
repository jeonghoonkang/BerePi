FLEET_DOMAIN=fleet.example.com
# openssl rand -hex 32
FLEET_HMAC_SECRET=CHANGE_ME_WITH_AT_LEAST_32_RANDOM_CHARACTERS
# openssl rand -hex 32
ADMIN_TOKEN=CHANGE_ME_WITH_A_DIFFERENT_RANDOM_TOKEN
# Device enrollment only: generate another secret with openssl rand -hex 32.
# Leave empty to disable new enrollment while keeping existing clients working.
FLEET_ENROLLMENT_TOKEN=
# Compare distinct installations that reported within the last 24 hours.
FLEET_ID_CONFLICT_WINDOW_SECONDS=86400
